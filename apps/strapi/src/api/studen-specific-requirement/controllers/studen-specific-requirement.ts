/**
 * studen-specific-requirement controller
 */

import { factories } from "@strapi/strapi"

export default factories.createCoreController(
  "api::studen-specific-requirement.studen-specific-requirement"
)
