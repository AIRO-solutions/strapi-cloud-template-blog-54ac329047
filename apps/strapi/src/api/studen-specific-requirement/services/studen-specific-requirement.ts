/**
 * studen-specific-requirement service
 */

import { factories } from "@strapi/strapi"

export default factories.createCoreService(
  "api::studen-specific-requirement.studen-specific-requirement"
)
