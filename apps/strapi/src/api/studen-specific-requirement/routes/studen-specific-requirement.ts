/**
 * studen-specific-requirement router
 */

import { factories } from "@strapi/strapi"

export default factories.createCoreRouter(
  "api::studen-specific-requirement.studen-specific-requirement"
)
